# AUTH v0.3 Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate the supplied AUTH v0.3 R2 implementation onto canonical B2 commit `708af3a2792102bbefb62c32505c0a84394a1d9e`, address every blocking review item, and publish only `feature/auth-v0.3-integration` to GitHub and GitVerse.

**Architecture:** Preserve the existing Express/Prisma backend and static frontend architecture. Add mirrored backend/browser password-policy modules with an explicit parity contract, extend registration validation and structured error handling, and improve only the existing account pages and session UX. Keep all deployment, database schema, Telegram, travel, map, weather, OCR, Plan B, invitation, notification, and SOS code unchanged.

**Tech Stack:** Node.js, Express, Prisma, SQLite for isolated local E2E only, browser JavaScript, Node test runner, Python/pytest for Telegram regression, Git/Gitleaks.

---

## Task 1: Establish the isolated integration baseline

- [ ] Confirm the worktree is on `feature/auth-v0.3-integration` at the exact canonical B2 commit.
- [ ] Confirm `origin/main`, `gitverse/main`, and `v0.2.0-b2` still resolve to the expected base.
- [ ] Record the archive SHA-256 and verify it contains no unsafe paths or forbidden files.
- [ ] Run the baseline backend, frontend, Prisma, and Telegram suites before changes.

## Task 2: Import AUTH v0.3 tests first

**Files:**

- Create: `backend/test/password-policy.test.js`
- Create: `frontend/tests/auth-registration-ui.test.cjs`
- Create: `frontend/tests/auth-remember-session.test.cjs`

- [ ] Add the supplied R2 tests without production code.
- [ ] Run focused backend and frontend tests and confirm the expected RED state is caused by the missing AUTH v0.3 implementation.

## Task 3: Integrate the backend password policy and registration contract

**Files:**

- Create: `backend/src/services/passwordPolicy.js`
- Modify: `backend/src/routes/auth.js`

- [ ] Add normalization, password evaluation, strength scoring, and stable validation error codes.
- [ ] Return structured registration `errors[]` while preserving the legacy top-level `error` field.
- [ ] Normalize email consistently for registration and login.
- [ ] Run focused password-policy and auth tests.
- [ ] Run the complete backend suite.

## Task 4: Integrate the account frontend

**Files:**

- Create: `frontend/assets/js/password-policy.js`
- Modify: `frontend/assets/js/account-pages.js`
- Modify: `frontend/assets/js/auth-storage.js`
- Modify: `frontend/assets/css/account-pages.css`
- Modify: `frontend/register.html`
- Modify: `frontend/login.html`
- Modify: `frontend/password-recovery.html`
- Modify: `frontend/profile.html`
- Modify: `frontend/dev-preview/account-pages-preview.html`

- [ ] Load the browser password policy before account-page logic.
- [ ] Add password rules, strength feedback, confirmation, busy-state protection, and session persistence.
- [ ] Remove automatic demo-login behavior and misleading consent/recovery claims.
- [ ] Preserve current page structure and unrelated application behavior.
- [ ] Run supplied frontend AUTH tests.

## Task 5: Address review findings with regression tests

**Files:**

- Modify: `frontend/tests/auth-registration-ui.test.cjs`
- Modify: `frontend/tests/auth-remember-session.test.cjs`
- Modify: `frontend/assets/js/account-pages.js`
- Modify: `frontend/assets/js/password-policy.js`
- Modify: `frontend/dev-preview/account-pages-preview.html`

- [ ] Add a test proving the two required Russian mail phrases are valid UTF-8 and changed source contains no U+FFFD.
- [ ] Add a live static-server test proving the dev-preview policy URL returns HTTP 200 and defines `window.TravelPasswordPolicy`.
- [ ] Add a test proving missing policy blocks registration with `Не удалось загрузить проверку пароля. Обновите страницу` and never shows false-green rules.
- [ ] Expand the parity test to compare constants, word lists, email regex source/flags, normalizers, evaluator behavior, primary error codes, and strength levels without depending on local variable names.
- [ ] Add tests for structured backend field-error mapping, unknown-error fallback, legacy `{error}` compatibility, and punctuation handling.
- [ ] Apply the minimum frontend fixes required to make those tests pass.

## Task 6: Run isolated local AUTH E2E

- [ ] Create a temporary SQLite database under the local backend directory only.
- [ ] Apply the existing Prisma schema to the temporary database.
- [ ] Start the backend and frontend static server on local ephemeral ports without production secrets.
- [ ] Verify registration, normalization, validation errors, login, logout, session restoration, remember-session behavior, duplicate submit protection, long-password handling, and legacy/structured error behavior.
- [ ] Exercise account pages at representative desktop and mobile viewport widths.
- [ ] Verify the dev preview loads the canonical password policy with no 404 and no duplicate implementation.
- [ ] Stop local processes and remove the temporary database and transient output.

## Task 7: Full regression and repository hygiene

- [ ] Run `npm ci`, Prisma generate/validate, and the full backend suite.
- [ ] Run the full frontend suite and confirm at least 36 tests.
- [ ] Run the full Telegram pytest suite without starting polling.
- [ ] Run Gitleaks and scans for `.env`, secrets, databases, logs, backups, user documents, temporary files, and U+FFFD.
- [ ] Review the final name-status and numeric diff, confirming no product files were deleted and no out-of-scope subsystem changed.

## Task 8: Documentation, commits, and dual-remote publication

**Files:**

- Create: `HANDOFF_AUTH_V0.3.md`
- Create: `DIFF_SUMMARY_AUTH_V0.3.md`

- [ ] Document only the implementation actually delivered, review fixes actually made, and tests actually run.
- [ ] Create meaningful non-secret commits on `feature/auth-v0.3-integration`.
- [ ] Fetch both remotes again and confirm their `main` branches remain unchanged.
- [ ] Push the feature branch to GitHub and GitVerse without force and without changing `main` or tags.
- [ ] Verify both remote feature refs resolve to the same final commit.
