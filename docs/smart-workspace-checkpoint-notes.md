
## Browser preview diagnostics

The canonical `trip-overview.html` auth guard correctly redirected an unauthenticated browser to the local login form, proving that the Smart Workspace route preserves existing access control rather than bypassing it. The standalone account fixture in `account-state-adapter.js` defines the actual local preview credential as `artem@example.test` / `Travel2026!`; the older frontend README password does not match this fixture. The visual verification route will therefore use this local demo account and the explicit `env=development&preview=smart-workspace&smartState=<state>` gate. No production auth, backend endpoint, or persisted Plan B mutation is involved.

A second browser pass confirmed that the standalone static server runs the account layer in integration mode rather than fixture-preview mode. As a result, even the fixture credential is rejected when no backend authentication service is present. This is expected outside the Smart Workspace scope and confirms that the new explicit Smart Workspace preview gate does not bypass the canonical auth guard. The next visual-verification step must inject a local test session using the same stored application state shape that the existing application bootstrap accepts, rather than modifying production authentication code.
Desktop visual checks: Plan B shows three externally supplied candidate references, explicit DEMO wording, candidate labels, preferences, and disabled Apply before selection. After Apply shows only a presentation timeline and Revert state available; no network action was triggered.

Mobile 390px visual inspection used a 390×2200 source screenshot, sliced into four ordered vertical tiles with 11% overlap. Tile 1 preserves the heading, factual route grid, and disruption banner without horizontal overflow. Tile 2 shows full-width primary/secondary actions and independent map/timeline cards. Tile 3 shows weather and documents stacked cleanly. Tile 4 preserves the candidate section and sticky disabled Apply presentation; the sticky bar is intentionally present at the viewport bottom, while the document is vertically scrollable. Across overlap boundaries, no clipped text, horizontal scrolling, or layout break was observed.
Post-fix desktop Plan B verification: absent map, weather, and documents no longer render as placeholder cards; only factual route/timeline and Plan B presentation remain.

Final mobile v2 inspection, tiles 1–2: the top factual grid and disruption card remain readable at 390px, primary and secondary actions have full width, and the formerly empty map/weather/documents cards are absent. The timeline transitions directly into the demo reminder and candidate section without overflow.

Mobile After Apply v2 inspection used two ordered 390px tiles. The lime confirmation banner, presentation-only Revert control, status text, and cancellation timeline are readable and fit the viewport. The second tile contains only the expected tail of the timeline and page background; there is no overflow or hidden additional factual card.
